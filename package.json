{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "cd examples/nextjs && npm run dev",
    "build": "npm run build:client && npm run build:generator && npm run build:incept && npm run build:react && npm run build:tailwind && npm run build:terminal && npm run build:translate && npm run build:types",
    "build:client": "cd packages/client && npm run build",
    "build:generator": "cd packages/generator && npm run build",
    "build:incept": "cd packages/incept && npm run build",
    "build:react": "cd packages/react && npm run build",
    "build:tailwind": "cd packages/tailwind && npm run build",
    "build:terminal": "cd packages/terminal && npm run build",
    "build:translate": "cd packages/type && npm run build",
    "build:types": "cd packages/type && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}