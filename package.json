{
  "private": true,
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "dev": "cd example && npm run dev",
    "build": "npm run build:ui",
    "build:ui": "cd packages/ui && npm run build"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  }
}